{
  "name": "policy-as-code",
  "description": "Create an AWS project that deploys: A VPC with private/public subnets An EC2 instance in the private subnet An S3 bucket for logs A restricted IAM role",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "dependencies": {
    "@pulumi/aws": "^7.11.1",
    "@pulumi/eks": "^4.0.1",
    "@pulumi/kubernetes": "^4.24.0",
    "@pulumi/pulumi": "*"
  }
}
